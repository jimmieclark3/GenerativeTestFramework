name: Build and Release

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]

env:
  DOTNET_VERSION: '8.0.x'
  SOLUTION_FILE: 'ReverseCoverage.sln'

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore dependencies
      run: dotnet restore ${{ env.SOLUTION_FILE }}

    - name: Build
      run: dotnet build ${{ env.SOLUTION_FILE }} --configuration Release --no-restore

    - name: Test
      run: dotnet test ${{ env.SOLUTION_FILE }} --configuration Release --no-build --verbosity normal --results-directory ./TestResults --logger "trx;LogFileName=test-results.trx"

    - name: Publish test results
      uses: dorny/test-reporter@v1
      if: always()
      with:
        name: Test Results
        path: 'TestResults/*.trx'
        reporter: dotnet-trx
        fail-on-error: false

    - name: Publish artifacts
      if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
      run: |
        dotnet publish src/ReverseCoverage.Orchestrator/ReverseCoverage.Orchestrator.csproj \
          --configuration Release \
          --output ./publish/orchestrator \
          --no-restore \
          --no-build
        
        dotnet publish src/ReverseCoverage.CoverageRunner/ReverseCoverage.CoverageRunner.csproj \
          --configuration Release \
          --output ./publish/coverage-runner \
          --no-restore \
          --no-build

    - name: Upload build artifacts
      if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
      uses: actions/upload-artifact@v4
      with:
        name: build-artifacts
        path: ./publish/
        retention-days: 30

  release:
    name: Create Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Get version from tag
      id: get_version
      run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

    - name: Build Release
      run: |
        dotnet restore ${{ env.SOLUTION_FILE }}
        dotnet build ${{ env.SOLUTION_FILE }} --configuration Release --no-restore

    - name: Create release packages
      run: |
        # Create output directory
        mkdir -p ./release-packages
        
        # Package Orchestrator
        dotnet publish src/ReverseCoverage.Orchestrator/ReverseCoverage.Orchestrator.csproj \
          --configuration Release \
          --output ./release-packages/orchestrator \
          -p:PublishSingleFile=true \
          -p:PublishTrimmed=false \
          --self-contained false
        
        # Package CoverageRunner
        dotnet publish src/ReverseCoverage.CoverageRunner/ReverseCoverage.CoverageRunner.csproj \
          --configuration Release \
          --output ./release-packages/coverage-runner \
          -p:PublishSingleFile=true \
          -p:PublishTrimmed=false \
          --self-contained false
        
        # Create zip files
        cd release-packages
        zip -r ReverseCoverage-Orchestrator-v${{ steps.get_version.outputs.VERSION }}.zip orchestrator/
        zip -r ReverseCoverage-CoverageRunner-v${{ steps.get_version.outputs.VERSION }}.zip coverage-runner/
        cd ..

    - name: Generate release notes
      id: release_notes
      run: |
        # Get the previous tag
        PREV_TAG=$(git describe --abbrev=0 --tags $(git rev-list --tags --skip=1 --max-count=1) 2>/dev/null || echo "")
        
        if [ -z "$PREV_TAG" ]; then
          echo "NOTES=Initial release of ReverseCoverage Test Synthesis Framework" >> $GITHUB_OUTPUT
        else
          # Generate changelog
          CHANGELOG=$(git log ${PREV_TAG}..HEAD --pretty=format:"- %s" --no-merges)
          echo "NOTES<<EOF" >> $GITHUB_OUTPUT
          echo "## Changes since ${PREV_TAG}" >> $GITHUB_OUTPUT
          echo "" >> $GITHUB_OUTPUT
          echo "${CHANGELOG}" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        fi

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        name: Release v${{ steps.get_version.outputs.VERSION }}
        body: ${{ steps.release_notes.outputs.NOTES }}
        draft: false
        prerelease: false
        files: |
          ./release-packages/*.zip
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Upload release artifacts
      uses: actions/upload-artifact@v4
      with:
        name: release-packages-v${{ steps.get_version.outputs.VERSION }}
        path: ./release-packages/*.zip
        retention-days: 90
